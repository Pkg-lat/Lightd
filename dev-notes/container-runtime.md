**Container Runtime**

When a container is alive, the states.json should track uptime aswell. screw websocket uptime, send container uptime.

**Websockets for console**

Make one for the attached process and another one for the normal container itself. 

**Container Logs**

Should be able to retrieve logs for a container.
and u should be able to send an action to the websocket to get old logs.





